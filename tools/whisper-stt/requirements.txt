# Runtime dependencies for the local faster-whisper and opt-in diarization paths.
# The pyannote 3.1 pipeline is intentionally held to its compatible release;
# model weights are separately consent-gated and never downloaded by inference.
faster-whisper>=1.1,<2
huggingface-hub>=0.23,<1
pyannote.audio==3.1.1
