{
  "schemaVersion": 1,
  "comment": [
    "Temporary suppressions for the dependency gates. Every entry MUST carry an",
    "expiry; the gate fails once an entry is past it, so a suppression cannot",
    "quietly become permanent. Removing the underlying finding is always",
    "preferable to extending the date.",
    "",
    "vulnerabilities[]: { package, advisoryUrl, reason, expiresUtc, owner }",
    "deprecations[]:    { package, reason, expiresUtc, owner }"
  ],
  "vulnerabilities": [],
  "deprecations": []
}
