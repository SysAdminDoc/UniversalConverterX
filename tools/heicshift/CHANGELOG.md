# Changelog

All notable changes to HEICShift will be documented in this file.

## [v2.8.0] - %Y->- (HEAD -> master, origin/master)

- Added: Add screenshot to README
- Added: Add live scan feedback with per-directory progress, status bar updates, and pulsing progress bar
- Fixed: Fix XMP metadata for AVIF/JXL, add UI scaling for diverse screen sizes
- Added: Add JPEG XL output, CLI full parity, progress counters, sorted scans
- Added: Add AVIF output, CSV export, drag & drop files, CLI parity flags, wall-clock time
- Added: Add JPEG/PNG input support — universal cross-format converter
- Added: Add CLI mode, disk space check, strip metadata, auto-open output, title bar file count, resize guard, better warnings, stats color reset, dependency version logging
- Added: Add atomic writes, output validation, dark title bar, presets, smart option visibility, log context menu, overlap guard, speed stats
- Added: Add image resize, filename prefix/suffix, progressive JPEG, lossless WebP, recent dirs, dark scrollbars
- Added: Add drag & drop, format filter, skip existing, EXIF auto-rotate, ETA progress, tray notifications, log export, PyInstaller CI/CD
