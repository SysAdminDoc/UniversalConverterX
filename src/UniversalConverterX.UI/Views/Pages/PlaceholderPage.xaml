<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UniversalConverterX.UI.Views.Pages.PlaceholderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Grid Padding="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="6">
            <TextBlock x:Name="ModuleTitle" Text="Module" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock x:Name="ModuleSubtitle" Text="Module description"
                       Style="{StaticResource SubtitleTextStyle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,28,0,0"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="18" MaxWidth="620">
                <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="48"
                        Width="96" Height="96" HorizontalAlignment="Center">
                    <FontIcon x:Name="ModuleIcon" Glyph="&#xE713;" FontSize="44"
                              Foreground="{StaticResource AccentBlueBrush}"/>
                </Border>

                <Border Background="{StaticResource SurfaceWarmBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="14"
                        Padding="12,5"
                        HorizontalAlignment="Center">
                    <TextBlock x:Name="StatusBadge" Text="Not available yet"
                               Style="{StaticResource PillTextStyle}"
                               Foreground="{StaticResource AccentYellowBrush}"/>
                </Border>

                <TextBlock x:Name="HeadlineText" Text="Tool not available yet"
                           Style="{StaticResource TitleTextStyle}"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>

                <TextBlock x:Name="DescriptionText"
                           Text="This tool is tracked, but its runnable workflow is not wired into this build."
                           TextAlignment="Center"
                           Style="{StaticResource SubtitleTextStyle}"
                           TextWrapping="Wrap"/>

                <Border x:Name="PoweredByBox" Visibility="Collapsed"
                        Background="{StaticResource SurfaceLightBrush}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                        CornerRadius="8" Padding="14,10" Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE71B;" FontSize="14"
                                  Foreground="{StaticResource AccentGreenBrush}"
                                  VerticalAlignment="Center"/>
                        <TextBlock x:Name="PoweredByText" Text=""
                                   Style="{StaticResource MutedTextStyle}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource SurfaceMutedBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="18"
                        Padding="18"
                        Margin="0,6,0,0"
                        MaxWidth="520">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Readiness requirements"
                                   Style="{StaticResource PanelTitleTextStyle}"/>
                        <TextBlock Text="A tool is marked Ready only when import, progress, cancellation, export, failure recovery, and local-processing details are wired."
                                   Style="{StaticResource MutedTextStyle}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center" Margin="0,8,0,0">
                    <Button Content="Browse Tools"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="OpenToolbox_Click"
                        AutomationProperties.AutomationId="PlaceholderPageButton01"/>
                    <Button Content="Back to Home"
                            Style="{StaticResource GhostButtonStyle}"
                            Click="OpenHome_Click"
                        AutomationProperties.AutomationId="PlaceholderPageButton02"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
