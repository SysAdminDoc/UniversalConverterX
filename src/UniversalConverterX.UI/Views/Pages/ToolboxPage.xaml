<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UniversalConverterX.UI.Views.Pages.ToolboxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Page.Resources>
        <DataTemplate x:Key="ToolboxTileTemplate">
            <Border Width="224" Height="142" Padding="16" Margin="0,0,12,12"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{StaticResource SurfaceLightBrush}" CornerRadius="12"
                                Width="40" Height="40">
                            <FontIcon Glyph="{Binding Glyph}" FontSize="16"
                                      Foreground="{Binding AccentBrush}"/>
                        </Border>
                        <Border Grid.Column="2" Background="{StaticResource SurfaceLightBrush}" CornerRadius="10"
                                Padding="6,2" Visibility="{Binding ShowAi}">
                            <TextBlock Text="AI" FontSize="10" FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBlueBrush}"/>
                        </Border>
                    </Grid>
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" Spacing="5" Margin="0,10,0,0">
                        <TextBlock Text="{Binding Title}" Style="{StaticResource LabelTextStyle}"
                                   TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Description}" FontSize="11"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   TextWrapping="Wrap" MaxLines="2"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="7">
                        <Ellipse Width="7" Height="7"
                                 Fill="{Binding StatusBrush}"
                                 VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding StatusBadge}" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{Binding StatusBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled">
        <StackPanel Padding="32" Spacing="26">
            <Border Background="{StaticResource HeroGradientBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="24"
                    Padding="32">
                <Grid ColumnSpacing="28">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="9">
                        <TextBlock Text="TOOLBOX" Style="{StaticResource EyebrowTextStyle}"/>
                        <TextBlock Text="Specialized tools for finishing, repairing, extracting, and preparing media."
                                   Style="{StaticResource TitleTextStyle}"
                                   TextWrapping="Wrap"
                                   MaxWidth="720"/>
                        <TextBlock Text="Ready tools open directly. Planned tools stay disabled and explain what is missing."
                                   Style="{StaticResource SubtitleTextStyle}"
                                   TextWrapping="Wrap"
                                   MaxWidth="760"/>
                    </StackPanel>
                    <Border Grid.Column="1"
                            Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="20"
                            Padding="22">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Status key" Style="{StaticResource PanelTitleTextStyle}"/>
                            <Grid RowSpacing="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentGreenBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="Ready opens a runnable workflow" Style="{StaticResource MutedTextStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentOrangeBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="Planned has a defined implementation path" Style="{StaticResource MutedTextStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource AccentYellowBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="Future still needs scope decisions" Style="{StaticResource MutedTextStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="10">
                                    <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="8" Padding="6,2">
                                        <TextBlock Text="AI" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="{StaticResource AccentBlueBrush}"/>
                                    </Border>
                                    <TextBlock Text="Model-backed tool" Style="{StaticResource MutedTextStyle}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Spacing="12">
                <TextBlock Text="Image" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="ImageGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Video" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="VideoGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="AI Tools" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="AiGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Audio" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="AudioGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Documents" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="DocumentGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Disc" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="DiscGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Other" Style="{StaticResource SectionHeaderTextStyle}"/>
                <GridView x:Name="OtherGrid" SelectionMode="None" IsItemClickEnabled="True"
                          ItemClick="Tile_Click" ItemTemplate="{StaticResource ToolboxTileTemplate}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
