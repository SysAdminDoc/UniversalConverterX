<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="UniversalConverterX.UI.Views.Pages.EbookConverterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Page.Resources>
        <DataTemplate x:Key="EbookFileTemplate">
            <Border Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="14"
                    Margin="0,0,0,10">
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE82D;" FontSize="24"
                              Foreground="{StaticResource AccentBlueBrush}"
                              VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                        <TextBlock Text="{Binding FileName}"
                                   Style="{StaticResource LabelTextStyle}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Path}"
                                   Style="{StaticResource MutedTextStyle}"
                                   FontSize="11"
                                   LineHeight="16"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <TextBlock Grid.Column="2"
                               Text="{Binding StatusText, Mode=OneWay}"
                               Style="{StaticResource MutedTextStyle}"
                               FontSize="11"
                               TextAlignment="Right"
                               VerticalAlignment="Center"/>
                    <Button x:Uid="EbookConverterPage_Item_001" Grid.Column="3"
                            Style="{StaticResource IconButtonStyle}"
                            AutomationProperties.Name="Remove from queue"
                            ToolTipService.ToolTip="Remove from queue"
                            Click="RemoveFile_Click"
                            Tag="{Binding}">
                        <FontIcon Glyph="&#xE711;" FontSize="14"
                                  Foreground="{StaticResource TextMutedBrush}"/>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="8">
                <TextBlock x:Uid="EbookConverterPage_Item_002" Text="EBOOK CONVERTER"
                           Style="{StaticResource EyebrowTextStyle}"/>
                <TextBlock x:Uid="EbookConverterPage_Item_003" Text="Convert EPUB / MOBI / AZW3 / PDF / FB2 / DOCX / HTML and more via Calibre's ebook-convert. Optional metadata overrides for title / authors / language."
                           Style="{StaticResource TitleTextStyle}"
                           TextWrapping="Wrap"
                           MaxWidth="720" LineHeight="30"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <Button x:Uid="EbookConverterPage_EbookConverterPageButton01" Content="Add eBooks"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="AddFiles_Click"
                    AutomationProperties.AutomationId="EbookConverterPageButton01"/>
                <Button x:Uid="EbookConverterPage_ConvertButton" x:Name="ConvertButton"
                        Content="Convert"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="Convert_Click"
                        IsEnabled="False"
                    AutomationProperties.AutomationId="ConvertButton"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Uid="EbookConverterPage_FormatCombo" x:Name="FormatCombo"
                          Header="Target format"
                          SelectedIndex="0"
                    AutomationProperties.AutomationId="FormatCombo">
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_007" Content="EPUB" Tag="epub"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_008" Content="MOBI" Tag="mobi"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_009" Content="AZW3 (Kindle)" Tag="azw3"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_010" Content="PDF" Tag="pdf"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_011" Content="FB2" Tag="fb2"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_012" Content="DOCX" Tag="docx"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_013" Content="ODT" Tag="odt"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_014" Content="HTML" Tag="html"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_015" Content="HTMLZ" Tag="htmlz"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_016" Content="RTF" Tag="rtf"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_017" Content="TXT" Tag="txt"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_018" Content="LIT" Tag="lit"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_019" Content="LRF" Tag="lrf"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_020" Content="PDB" Tag="pdb"/>
                    <ComboBoxItem x:Uid="EbookConverterPage_Item_021" Content="CBZ" Tag="cbz"/>
                </ComboBox>
                <TextBox x:Uid="EbookConverterPage_OutputDirBox" Grid.Column="1"
                         x:Name="OutputDirBox"
                         Header="Output directory"
                         IsReadOnly="True"
                         PlaceholderText="(same as source)"
                    AutomationProperties.AutomationId="OutputDirBox"/>
                <Button x:Uid="EbookConverterPage_EbookConverterPageButton02" Grid.Column="2"
                        Content="Browse..."
                        Style="{StaticResource SecondaryButtonStyle}"
                        VerticalAlignment="Bottom"
                        Click="BrowseOutputDir_Click"
                    AutomationProperties.AutomationId="EbookConverterPageButton02"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="14">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Uid="EbookConverterPage_TitleBox" x:Name="TitleBox"
                         Header="Title (optional)"
                         PlaceholderText="(keep source)"
                    AutomationProperties.AutomationId="TitleBox"/>
                <TextBox x:Uid="EbookConverterPage_AuthorsBox" Grid.Column="1"
                         x:Name="AuthorsBox"
                         Header="Authors (optional)"
                         PlaceholderText='Asimov, Isaac &amp; Clarke, Arthur'
                    AutomationProperties.AutomationId="AuthorsBox"/>
                <TextBox x:Uid="EbookConverterPage_LanguageBox" Grid.Column="2"
                         x:Name="LanguageBox"
                         Header="Language (ISO-639)"
                         PlaceholderText="en"
                    AutomationProperties.AutomationId="LanguageBox"/>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="14">
            <Grid>
                <StackPanel x:Name="EmptyState"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Spacing="10">
                    <Border Width="80" Height="80" CornerRadius="20"
                            Background="{StaticResource SurfaceLightBrush}">
                        <FontIcon Glyph="&#xE82D;" FontSize="36"
                                  Foreground="{StaticResource AccentBlueBrush}"/>
                    </Border>
                    <TextBlock x:Uid="EbookConverterPage_Item_027" Text="Add eBooks"
                               Style="{StaticResource SubtitleTextStyle}"
                               HorizontalAlignment="Center"/>
                    <TextBlock x:Uid="EbookConverterPage_Item_028" Text="EPUB, MOBI, AZW3, PDF, FB2, DOCX, HTML, RTF, TXT. Pick a target format and run."
                               Style="{StaticResource MutedTextStyle}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center" TextWrapping="Wrap"
                               MaxWidth="420"/>
                </StackPanel>
                <ScrollViewer x:Name="FilesScroll" Visibility="Collapsed"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="FilesList"
                                  ItemTemplate="{StaticResource EbookFileTemplate}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <ProgressBar x:Uid="EbookConverterPage_WorkProgress" AutomationProperties.Name="Progress"
                     Grid.Row="4"
                     x:Name="WorkProgress"
                     Minimum="0" Maximum="100"
                     IsIndeterminate="False"/>

        <ScrollViewer Grid.Row="5" Height="80"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="LogText"
                       Style="{StaticResource MutedTextStyle}"
                       FontFamily="Consolas, Cascadia Mono, monospace"
                       FontSize="11"
                       TextWrapping="Wrap"/>
        </ScrollViewer>

        <TextBlock x:Uid="EbookConverterPage_StatusText" Grid.Row="6"
                   x:Name="StatusText"
                   Style="{StaticResource MutedTextStyle}"
                   Text="Add eBooks to begin."/>
    </Grid>
</Page>
